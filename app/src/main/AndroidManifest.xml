<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tv"
        android:label="TMDbz"
        android:roundIcon="@drawable/tv"
        android:supportsRtl="true"
        android:theme="@style/Theme.saz_ppb_prak7"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.ChatFragment"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.register"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.MainActivity"
            android:exported="false" />
    </application>

</manifest>